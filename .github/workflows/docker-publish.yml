1|name: Docker
2|
3|# This workflow uses actions that are not certified by GitHub.
4|# They are provided by a third-party and are governed by
5|# separate terms of service, privacy policy, and support
6|# documentation.
7|
8|on:
9|  schedule:
10|    - cron: '33 23 * * *'
11|  push:
12|    branches: [ "main" ]
13|    # Publish semver tags as releases.
14|    tags: [ 'v*.*.*' ]
15|  pull_request:
16|    branches: [ "main" ]
17|
18|env:
19|  REGISTRY: docker.io
20|  IMAGE_NAME: ${{ secrets.DOCKER_HUB_USERNAME }}/${{ github.event.repository.name }}
21|
22|
23|jobs:
24|  build:
    if: false  # disabled — Docker Hub credentials need updating
25|
26|    runs-on: ubuntu-latest
27|    permissions:
28|      contents: read
29|      packages: write
30|      # This is used to complete the identity challenge
31|      # with sigstore/fulcio when running outside of PRs.
32|      id-token: write
33|
34|    steps:
35|      - name: Checkout repository
36|        uses: actions/checkout@v6
37|
38|      # Install the cosign tool except on PR
39|      # https://github.com/sigstore/cosign-installer
40|      - name: Install cosign
41|        if: github.event_name != 'pull_request'
42|        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 #v4.1.2
43|        with:
44|          cosign-release: 'v2.2.4'
45|
46|      # Set up BuildKit Docker container builder to be able to build
47|      # multi-platform images and export cache
48|      # https://github.com/docker/setup-buildx-action
49|      - name: Set up Docker Buildx
50|        uses: docker/setup-buildx-action@f95db51fddba0c2d1ec667646a06c2ce06100226 # v3.0.0
51|
52|      # Login to GitHub Container Registry (GHCR) using the GITHUB_TOKEN
53|      - name: Log into GitHub Container Registry
54|        if: ${{ env.REGISTRY == 'ghcr.io' && github.event_name != 'pull_request' }}
55|        uses: docker/login-action@v3
56|        with:
57|          registry: ghcr.io
58|          username: ${{ github.actor }}
59|          password: ${{ secrets.GITHUB_TOKEN }}
60|
61|      # Login to Docker Hub using repository secrets (DOCKER_HUB_USERNAME and DOCKER_HUB_TOKEN)
62|      - name: Log into Docker Hub
63|        if: ${{ env.REGISTRY == 'docker.io' && github.event_name != 'pull_request' }}
64|        uses: docker/login-action@v3
65|        with:
66|          registry: docker.io
67|          username: ${{ secrets.DOCKER_HUB_USERNAME }}
68|          password: ${{ secrets.DOCKER_HUB_TOKEN }}
69|
70|      # Extract metadata (tags, labels) for Docker
71|      # https://github.com/docker/metadata-action
72|      - name: Extract Docker metadata
73|        id: meta
74|        uses: docker/metadata-action@80c7e94dd9b9319bd5eb7a0e0fe9291e23a2a2e9 # v6.1.0
75|        with:
76|          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
77|          tags: |
78|            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
79|            type=ref,event=tag
80|
81|      # Build and push Docker image with Buildx (don't push on PR)
82|      # https://github.com/docker/build-push-action
83|      - name: Build and push Docker image
84|        id: build-and-push
85|        uses: docker/build-push-action@f9f3042f7e2789586610d6e8b85c8f03e5195baf # v7.2.0
86|        with:
87|          context: .
88|          push: ${{ github.event_name != 'pull_request' }}
89|          tags: ${{ steps.meta.outputs.tags }}
90|          labels: ${{ steps.meta.outputs.labels }}
91|          cache-from: type=gha
92|          cache-to: type=gha,mode=max
93|
94|      # Sign the resulting Docker image digest except on PRs.
95|      # This will only write to the public Rekor transparency log when the Docker
96|      # repository is public to avoid leaking data.  If you would like to publish
97|      # transparency data even for private images, pass --force to cosign below.
98|      # https://github.com/sigstore/cosign
99|      - name: Sign the published Docker image
100|        if: ${{ github.event_name != 'pull_request' }}
101|        env:
102|          # https://docs.github.com/en/actions/security-guides/security-hardening-for-github-actions#using-an-intermediate-environment-variable
103|          TAGS: ${{ steps.meta.outputs.tags }}
104|          DIGEST: ${{ steps.build-and-push.outputs.digest }}
105|        # This step uses the identity token to provision an ephemeral certificate
106|        # against the sigstore community Fulcio instance.
107|        run: echo "${TAGS}" | xargs -I {} cosign sign --yes {}@${DIGEST}
108|      
109|      - name: Run Trivy vulnerability scanner
110|        uses: aquasecurity/trivy-action@v0.36.0
111|        with:
112|          image-ref: bkoltz/project-alpha:latest
113|          format: table
114|          output: trivy-report.txt
115|          exit-code: '0'          # don’t fail the build, just report
116|          ignore-unfixed: true    # ignore issues without upstream fixes
117|          vuln-type: 'os,library' # scan both OS packages and PHP libs
118|          severity: 'CRITICAL,HIGH,MEDIUM,LOW'
119|      - name: Upload Trivy report
120|        uses: actions/upload-artifact@v4
121|        with:
122|          name: trivy-report
123|          path: trivy-report.txt
124|
125|