# Historical Configuration Fixture

This directory is a retained test fixture from the pre-no-environment configuration migration. It is not the active container configuration volume and must not contain real credentials, encryption keys, or production settings.

Current container configuration is persisted in the `pa_config` named volume at `/var/www/config`.
